# SQL Academic Project : Velomax

- `Velomax_Final.sql` file is the SQL code that creates all the database structure.
- The C# project contains function which allows the user to manage the database.
